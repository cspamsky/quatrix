{
  "name": "quatrix-root",
  "version": "1.0.3-Beta",
  "description": "Quatrix - Advanced Game Server Management Panel for CS2",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "overrides": {
    "fast-xml-parser": "^5.3.4",
    "qs": "^6.14.2"
  },
  "scripts": {
    "dev": "npx concurrently --names \"SERVER,CLIENT\" --prefix-colors \"cyan.bold,magenta.bold\" --prefix \"[{name}]\" \"npm run dev --prefix server\" \"npx wait-on tcp:3001 && npm run dev --prefix client\"",
    "build": "npm run build:server && npm run build:client",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "start": "cross-env NODE_ENV=production npm start --prefix server",
    "clean": "rimraf dist && rimraf server/dist && rimraf client/dist",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "fix": "npx rimraf node_modules server/node_modules client/node_modules && npm run install-all",
    "test": "npm run test --prefix server"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.3",
    "@types/node-fetch": "^2.6.13",
    "@types/uuid": "^10.0.0",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "eslint": "^9.39.3",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.3.0",
    "prettier": "^3.8.1",
    "rimraf": "^6.1.3",
    "typescript-eslint": "^8.56.0",
    "wait-on": "^9.0.3"
  },
  "engines": {
    "node": ">=20.6.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "cs2",
    "game-server",
    "management-panel",
    "rcon",
    "steamcmd",
    "quatrix"
  ],
  "author": "cspamsky",
  "license": "MIT",
  "dependencies": {
    "uuid": "^13.0.0"
  }
}
